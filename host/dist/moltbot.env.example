# Copy to /etc/moltbot/moltbot.env and set strict permissions:
#   sudo install -d -m 0750 /etc/moltbot
#   sudo install -m 0640 -o root -g moltbot dist/moltbot.env.example /etc/moltbot/moltbot.env
#   sudoedit /etc/moltbot/moltbot.env
#
# Do NOT place MOLT_ENCRYPTION_STORAGE_KEK in this file in production.
# The KEK should be provided by security.encryption.storageKekCommand.
#
# Default command: /opt/moltbot/tee/gramine/get-storage-kek.sh
# Provision handle helper:
#   sudo /opt/moltbot/tee/gramine/provision-tpm-kek.sh --handle 0x81010020 --env-file /etc/moltbot/moltbot.env

# Optional explicit command override for prestart validation:
# MOLTBOT_STORAGE_KEK_COMMAND=/opt/moltbot/tee/gramine/get-storage-kek.sh

# TPM handle used by get-storage-kek.sh:
MOLTBOT_TPM2_KEK_HANDLE=0x81010020

# Optional TPM auth (use one):
# MOLTBOT_TPM2_KEK_AUTH=replace_me
# MOLTBOT_TPM2_KEK_AUTH_FILE=/etc/moltbot/tpm2-kek-auth.txt
